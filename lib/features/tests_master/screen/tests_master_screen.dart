import 'package:flutter/material.dart';

class TestsMasterScreen extends StatelessWidget {
  const TestsMasterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Test Master (Admin) - Categories, Subcategories, Tests, Panel Support'),
    );
  }
}
